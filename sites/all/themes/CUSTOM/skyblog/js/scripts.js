// We define a function that takes one parameter named $.
(function ($) {
  // Use jQuery with the shortcut:
  // console.log($.browser);
  // Here we immediately call the function with jQuery as the parameter.
  jQuery(document).ready(function($) {
  // Code that uses jQuery's $ can follow here.
//  	$('input[type=text]').addClass('form-control mr-sm-2');
//  	$('input[type=search]').addClass('form-control mr-sm-2');
//  	$('input[type=password]').addClass('form-control mr-sm-2');
//  	$('input[type=mail]').addClass('form-control mr-sm-2');
//  	$('input[type=submit]').addClass('btn btn-secondary');
	});
}(jQuery));
